"""Production RAG Lab — shared benchmark harness.

Phase 0 foundation. Everything downstream plugs into the stage contract in
`harness.contract` and is composed from a YAML config via `harness.config`.
"""

__version__ = "0.1.0"
