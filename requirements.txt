# Phase 0 core — light on purpose (harness + metrics + naive baseline run with just these).
numpy>=1.24
pyyaml>=6
pytest>=7
ruff>=0.4

# ── Optional, per-phase (uncomment as you reach the phase) ─────────────────────
# Phase 3 embeddings:      sentence-transformers>=2.2
# Phase 4 indexing:        faiss-cpu>=1.7  hnswlib>=0.7  rank-bm25>=0.2
# Phase 0/11 real datasets: datasets>=2.14  beir>=2.0
# Generation / judge:      anthropic[bedrock]>=0.40  python-dotenv>=1.0
# Phase 11 RAGAS:          ragas>=0.1
# Local model:             llama-cpp-python  (CMAKE_ARGS="-DGGML_METAL=on")
